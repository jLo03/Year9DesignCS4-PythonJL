state = 0

while True:
	if state == 0:
		#Beginning program 
		state = 1
		#Command to move to next stage

	elif state == 1:
		state == 0:
		#Greatest part about it, allows more fluidity and control than an program 
		#That just runs from top to bottom

		#Allows us to go back to the old stage and reuse the code like a function
		#But more effective because using functions makes it a lot messier 
		#As well as a state machine lets you correct error's such as
		#Wrong input - go back to previous or same state
